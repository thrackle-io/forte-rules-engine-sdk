const ethers = require('ethers');

export function parse(syntax: string) {

    // var str = "[ 1 + 1 == 2 ] AND [ 3 + 4 > 5 AND 1 == 1 ] --> revert --> addValue(uint256)"
    // var initialSplit = 
}