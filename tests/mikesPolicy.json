{
  "Policy": "Test Policy",
  "PolicyType": "open",
  "CallingFunctions": [
    {
      "name": "transfer(uint256 to, uint256 value, uint256 somethingElse)",
      "functionSignature": "transfer(uint256 to, uint256 value, uint256 somethingElse)",
      "encodedValues": "uint256 to, uint256 value, uint256 somethingElse"
    }
  ],
  "ForeignCalls": [],
  "Trackers": [
    {
      "name": "trackerOne",
      "type": "uint256",
      "initialValue": "1"
    },
    {
      "name": "trackerTwo",
      "type": "uint256",
      "initialValue": "2"
    }
  ],
  "MappedTrackers": [],
  "Rules": [
    {
      "condition": "value == 1234 AND value == 1234",
      "positiveEffects": ["revert(\"Tracker Updated\")"],
      "negativeEffects": ["revert(\"WE DID IT\")"],
      "callingFunction": "transfer(uint256 to, uint256 value, uint256 somethingElse)"
    }
  ]
}
